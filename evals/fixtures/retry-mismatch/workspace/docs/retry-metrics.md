# Retry metrics

The worker metric reports the number of retry attempts. The dashboard metric
reports the number of deliveries that needed one or more retry attempts. The
metrics have different units and must not be compared as equal totals.
